var fhserver = require('./fhserver.js');

var fh;
exports.FeedHenry = function(cfg, logger) {
  if(!fh && (!cfg || !logger)) {
    throw new Error('Bad constructor');
  }

  if(!fh) { 
    fh = new fhserver.FHServer(cfg, logger);
  }

  return fh;
};
